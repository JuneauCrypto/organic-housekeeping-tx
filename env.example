# Email Configuration
# These settings are for sending emails via SMTP
# You'll need to get these from your email provider

# SMTP Server Settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false

# Email Account Credentials
# For Gmail, you'll need to use an "App Password" instead of your regular password
# See: https://support.google.com/accounts/answer/185833
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password-here

# Email From Address (usually same as SMTP_USER)
SMTP_FROM=your-email@gmail.com

# Email To Address (where form submissions will be sent)
EMAIL_TO=info@organichousekeepingtx.com

# Server Port (optional, defaults to 3000)
PORT=3000
